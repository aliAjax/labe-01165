# 高校水电煤气管理信息系统

## How to Run

### 方式一：使用 Docker Compose（推荐）

```bash
# 构建并启动服务（后台运行）
docker compose up --build -d

# 运行程序进行交互
docker exec -it utility-management-system ./utility_system

# 程序中选择 0 退出
```

### 方式二：使用 Docker 直接构建

```bash
# 构建镜像
cd backend
docker build -t utility-management-system .

# 运行容器（交互模式）
docker run -it --rm utility-management-system ./utility_system
```

### 方式三：本地编译运行

```bash
cd backend
make
./utility_system
```

### 验证跨平台镜像（ARM64）

```bash
# 验证镜像是否支持 ARM64（苹果 M 系列芯片）
docker pull --platform linux/arm64 alpine:3.19

# 构建 ARM64 版本
docker build --platform linux/arm64 -t utility-management-system:arm64 ./backend
```

### 停止服务

```bash
docker compose down
```

## Services

本系统提供以下服务功能：

| 服务模块 | 功能描述 |
|----------|----------|
| 用户管理 | 添加教工/学生用户、查看用户列表、搜索用户 |
| 抄表录入 | 录入水表、电表、煤气表数据 |
| 费用查询 | 查询指定用户费用、查看所有费用记录、重新计算费用 |
| 缴费管理 | 用户缴费、查看未缴费名单 |
| 收费标准 | 查看和修改收费标准（单价、免费额度） |
| 初始化测试数据 | 加载预设的10个用户和15条抄表记录（可选择清空现有数据） |

## 测试账号

系统内置测试数据初始化功能（主菜单选择"6"），包含以下测试账号：

**注意**：如果系统中已有数据，初始化时会提示是否清空现有数据后重新初始化。

### 教工用户
| 用户ID | 姓名 | 工号 | 部门 | 地址 |
|--------|------|------|------|------|
| T001 | 张教授 | 20150001 | 计算机学院 | 教师公寓A101 |
| T002 | 李副教授 | 20160002 | 数学学院 | 教师公寓A202 |
| T003 | 王讲师 | 20180003 | 物理学院 | 教师公寓B103 |
| T004 | 刘教授 | 20140004 | 化学学院 | 教师公寓B204 |
| T005 | 陈副教授 | 20170005 | 外语学院 | 教师公寓C105 |

### 学生用户
| 用户ID | 姓名 | 学号 | 宿舍楼 | 房间 | 年级 |
|--------|------|------|--------|------|------|
| S001 | 小明 | 2021001001 | 学生公寓1号楼 | 101室 | 3 |
| S002 | 小红 | 2021001002 | 学生公寓1号楼 | 102室 | 3 |
| S003 | 小刚 | 2022001003 | 学生公寓2号楼 | 201室 | 2 |
| S004 | 小丽 | 2022001004 | 学生公寓2号楼 | 202室 | 2 |
| S005 | 小强 | 2023001005 | 学生公寓3号楼 | 301室 | 1 |

## 题目内容

4.13 水电煤气管理系统★
（1）问题描述
设计一个水电管理信息系统，能够对高校的水电费用进行管理,包括了登记费用，查询费用，以及
住户信息管理等。在设计时要考虑到学生和教工在用水电时的不同，学生可以免费使用一定额度的水
电，超过这个额度以后必须自费使用，且自费部分水电费的价格标准要高于教工的收费标准（主要是
节约资源）。
（2）基本要求
①实现对用户信息的录入；
②实现水电煤气数据的录入；
③计算并查询用户应缴费用；
④查询未缴纳费用名单。
（3）测试数据
可选用小区物业管理部门的数据，也可采用手工输入不少于10个用户信息的数据。
（4）实现提示
①用户基本信息类，教工用户信息类，学生用户信息类；
②收费标准类，该类存储水电煤气标准单位的收费标准，如：煤气，1.0元/立方米；
③不同类型人员水、电、煤气信息类，这些类可以包括水表，电表，煤气表ID,抄表时间，上次抄
表时间，本次抄表时间，上次抄表度数，本次抄表度数，本次使用度数，费用，是否缴费标记等信息。
在实现的时候考虑继承和多态技术的合理使用。用c++实现
---

## 项目结构

```
├── backend/                  # 后端项目目录
│   ├── main.cpp              # 主程序入口
│   ├── utility_system.h      # 头文件（类声明）
│   ├── utility_system.cpp    # 实现文件（类定义）
│   ├── Makefile              # 编译配置
│   └── Dockerfile            # Docker 构建文件（支持 ARM64/AMD64）
├── docker-compose.yml        # Docker Compose 配置
├── .gitignore                # Git 忽略文件配置
└── README.md                 # 项目说明文档
```

## 功能特点

### 1. 用户管理
- 支持添加教工用户（包含部门、工号信息）
- 支持添加学生用户（包含学号、宿舍楼、年级信息）
- 查看和搜索用户信息

### 2. 抄表数据录入
- 水表数据录入
- 电表数据录入
- 煤气表数据录入

### 3. 费用计算
- **教工**：直接按用量和单价计费
- **学生**：
  - 享有一定免费额度（默认：水3吨，电20度，煤气5立方米）
  - 超出免费额度部分按较高价格计费（体现节约资源理念）

### 4. 收费标准（默认值）
| 项目 | 教工单价 | 学生单价（超额部分） |
|------|----------|---------------------|
| 水费 | 3.0元/吨 | 4.5元/吨 |
| 电费 | 0.6元/度 | 0.8元/度 |
| 煤气 | 2.5元/m³ | 3.0元/m³ |

### 5. 缴费管理
- 支持按类型缴费（水费/电费/煤气费）
- 支持一次性缴清全部费用
- 查看未缴费名单

### 6. 数据初始化
- 内置测试数据初始化功能
- 初始化前会检查现有数据，避免重复
- 可选择清空现有数据后重新初始化

## 类设计结构

```
User (抽象基类)
├── StaffUser (教工用户类)
└── StudentUser (学生用户类)

MeterRecord (抄表记录基类)
├── WaterMeterRecord (水表记录类)
├── ElectricMeterRecord (电表记录类)
└── GasMeterRecord (煤气表记录类)

PriceStandard (收费标准类)
UtilityManagementSystem (管理系统主类)
```

## 设计亮点

1. **继承与多态**：
   - `User`类为抽象基类，定义了`display()`和`calculateFee()`等虚函数
   - `StaffUser`和`StudentUser`分别实现不同的费用计算逻辑

2. **差异化计费**：
   - 学生有免费额度，超出后按更高价格计费
   - 教工无免费额度，但单价较低

3. **模块化设计**：
   - 收费标准独立成类，便于修改和维护
   - 抄表记录使用继承结构，便于扩展

4. **安全输入处理**：
   - 使用安全输入函数处理用户输入
   - 防止无效输入导致程序异常

5. **跨平台Docker支持**：
   - 基于 Alpine Linux 构建，镜像体积小
   - 同时支持 ARM64（苹果M系列）和 AMD64（Intel/AMD）架构
   - 使用多阶段构建优化镜像大小

## 测试数据说明

系统内置测试数据初始化功能，包含：
- 5个教工用户
- 5个学生用户
- 5条水表记录
- 5条电表记录
- 5条煤气表记录

部分记录已设置为已缴费状态，可用于测试未缴费名单功能。

## Docker 相关命令

```bash
# 查看容器日志
docker compose logs -f backend

# 进入运行中的容器 Shell
docker exec -it utility-management-system /bin/sh

# 运行程序
docker exec -it utility-management-system ./utility_system

# 重新构建镜像
docker compose build --no-cache

# 停止并删除容器
docker compose down

# 清理未使用的镜像
docker image prune -f
```
